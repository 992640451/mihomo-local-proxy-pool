# 安全政策

## 支持范围

安全修复优先应用于最新发布版本和默认 Docker Compose 部署。项目默认只监听 `127.0.0.1`；修改绑定地址、反向代理或防火墙后的安全责任由部署者承担。

## 报告漏洞

请通过 GitHub 仓库的“Security → Report a vulnerability”私下报告。不要在公开 Issue、Discussion 或 Pull Request 中包含漏洞细节、订阅地址、节点凭据、控制器密钥或可用的会话 Cookie。

报告请包含：

- 受影响版本或提交。
- 部署方式和操作系统。
- 最小复现步骤及影响。
- 已脱敏的日志或请求。
- 可行的缓解方式（如有）。

在修复版本发布前，请避免公开披露可被直接利用的细节。

## 部署边界

- 管理页面登录认证不等同于代理端口认证。
- 默认代理端口只供本机使用，不能直接暴露到公网。
- `.env`、SQLite 数据库和 Mihomo 生成配置都可能包含敏感信息，应限制文件访问并纳入备份保护。
- 加密恢复包包含可还原的订阅和节点凭据，应使用强口令并与口令分开存放；脱敏诊断文件在公开前仍应由提交者复核。
- 反向代理使用不同浏览器来源时应通过 `APP_ALLOWED_ORIGINS` 精确列出来源，不要放宽为通配符。
- 发现密钥进入 Git 历史后，应立即轮换，而不只是删除最新提交中的文件。
