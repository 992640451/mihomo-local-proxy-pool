# M3：可观测性

适用于默认 Docker 和便携版的内置 Mihomo 模式。此里程碑不修改应用发布版本号，不包含 webhook 或外部通知。

## 使用

1. 在「节点」查看真实健康、最近延迟和检测时间。按订阅、国家或名称筛选，选择节点后点击「测速所选」。每页、每批最多 100 个节点，可取消正在执行的任务。
2. 在「可观测性」查看端口活动节点、近 24 小时成功率、小时失败趋势、连续异常检测次数，选择端口后查看历史或发起验证。原端口页的「验证」也会写入同一份历史。
3. 后台检测默认关闭。需要时在「可观测性 → 后台检测设置」开启，设置保存在本机数据库，重启后保留。

「健康」来自实际检测，不等同于「已导入」。无测量显示未检测，超过 `max(10 分钟, 检测周期 × 2)` 的结果显示过期。核心不可达或节点未加载时，保留的历史结果不会显示为当前健康。界面每 5 秒读取一次核心状态，隐藏页面暂停读取；读取不会触发节点测速。当前启用订阅中的节点会加载进核心以支持未分配端口的节点测速，但不会因此新增监听端口或后台检查组。

节点测速通过 [Mihomo Controller API](https://wiki.metacubex.one/api/) 的单节点 delay 接口执行，衡量 HTTP 请求延迟，不测量下载带宽。策略活动节点来自核心的 `now` 字段；负载均衡通常没有单一活动节点，界面不会推测。旧版外部核心/Clash Verge 部署不支持此检测页，原端口验证仍可使用。

## 流量与资源预算

| 设置 | 默认 | 范围 |
| --- | --- | --- |
| 后台检测 | 关闭 | 手动开启 |
| 周期 | 900 秒 | 300–86400 秒 |
| 节点并发 | 3 | 1–6 |
| 单次超时 | 5000 毫秒 | 1000–10000 毫秒 |
| 后台/可观测性页每端口连接数 | 2 | 2–8 |
| 历史保留 | 7 天 | 1–30 天 |
| 最大历史条数 | 20000 | 100–50000 |

全服务只运行一个检测任务。节点在任务内受并发限制；端口及其连接串行检测，每次使用独立代理连接。原端口页手动验证默认 8 次，接口支持 2–20 次。任务结束后统一冷却 15 秒，避免多标签页叠加。自动检测每轮最多 100 个节点与 10 个已启用端口，超出部分按轮次轮换，不补跑积压任务；下一轮从任务结束后计算周期（最多另有 30 秒调度扫描延迟）。停用后台检测会取消正在执行的后台任务，不影响手动任务。

单节点请求允许额外 1 秒接收核心超时响应。已有 Mihomo 策略组自身的健康检查独立运行，不受此页面的开关控制。

## 指标解释

- 成功率：最近 24 小时成功出口连接数 / 总出口连接数；节点测速不混入端口成功率。没有样本显示「—」，而不是 0%。
- 连续异常检测：保留期内同一目标从最新记录向前，连续存在失败连接的检测轮数；包含部分失败，不是连续失败连接数。
- 延迟：成功样本平均延迟；全失败显示「—」。
- 出口分布：每次验证的 IP、国家和次数。同出口 IP 不代表轮询失效。
- 小时趋势：最近 24 个整点小时桶，灰色无样本、绿色全成功、红色高度为失败比例。最左小时桶边界与滚动 24 小时总计窗口可能不同。
- 取消、进程退出或重启中断的未完成检测不会计为失败。已完成节点和端口的结果仍保留；重启不会自动重放中断任务。

端口历史附带当时的协议、策略和节点 ID。端口号复用后会延续该端口历史，应结合配置摘要判断。历史只保存手动/后台主动检测，Mihomo 自身的历史仅用于实时健康读取，不重复落库。

## 存储、安全与第三方请求

新增 `observability.sqlite` 默认位于订阅/会话数据库旁，与 Docker `/data` 持久化卷、便携版 `data` 目录一起保留。可通过 `OBSERVABILITY_DB` 指定路径。历史同时按年龄和条数限制清理，缩小保留范围会不可逆清理超出限制的记录。

仅在用户发起检测或开启定时检测后产生主动请求：节点测速默认访问 `https://www.gstatic.com/generate_204`，出口查询默认访问 `https://ipwho.is/`。提供者会看到代理出口 IP；结果只保存在本机，不上传遥测。HTTP、Mixed 和 SOCKS5 端口按各自协议验证，SOCKS5 使用代理端 DNS。

管理员可通过环境变量 `OBSERVABILITY_TEST_URL`、`EGRESS_LOOKUP_URL` 配置自己的测试服务；浏览器 API 不接受任意检测 URL。应使用自己信任、允许检测的服务，遵守其限额。Controller 只在后端访问，不把密钥或完整核心响应发送给前端。错误消息脱敏，响应体和历史负载均有容量上限。

出口 IP 会明文保存在本机检测历史中，数据库文件应受本机访问权限保护。订阅原文、节点密码和 Cookie 不进入检测历史。配置恢复包和诊断导出不包含检测样本或出口 IP，只有诊断组件状态和审计任务摘要；需要迁移历史时，应在停止服务后备份数据目录。

检测任务持有恢复互斥租约，即使批量接口已返回 202、浏览器关闭或离开页面，恢复也会等待任务结束/取消后才能执行。

## 接口与验证

所有接口继承管理登录和同源校验：

| 接口 | 作用 |
| --- | --- |
| `GET /api/observability` | 节点、端口、核心状态与近 24 小时摘要（短缓存） |
| `GET /api/observability/status` | 设置、调度状态、最近任务和冷却时间 |
| `PATCH /api/observability/settings` | 校验并持久化设置 |
| `POST /api/observability/nodes/test` | `{ "nodeIds": ["…"] }`，返回 202 与任务 ID |
| `POST /api/observability/cancel` | 取消当前检测任务 |
| `GET /api/observability/history?kind=port&targetId=17900` | 按目标查询历史；支持 `before` 游标和 `limit`（最多 100） |
| `POST /api/ports/:port/verify` | 原验证接口，支持 `attempts`，并写入端口检测历史 |

`npm test` 包含隔离的存储、Controller、调度器、鉴权、恢复互斥、HTTP 和 SOCKS5 模拟服务测试，不访问真实订阅。

浏览器回归可执行 `npm run build` 后运行 `node tests/helpers/observability-preview.mjs`，打开 `http://127.0.0.1:43021`。该工具使用临时数据库和纯本地模拟 Controller/代理，未启用登录；不可对外暴露。结束进程会清理该工具自己的临时数据。
