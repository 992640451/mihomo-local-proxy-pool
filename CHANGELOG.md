# 变更记录

简体中文 · [English](CHANGELOG_EN.md)

本项目按语义化版本管理公开版本。这里只记录面向使用者的行为变化；完整实现细节可查看 Git 提交历史。

## [未发布]

## [1.3.0] - 2026-09-03

### 新增

- 侧栏底部与系统设置提供版本更新入口、新版本提示、发布说明、稍后提醒和忽略版本；自动检查可以关闭，不会自动安装。
- 管理员确认后由独立更新器下载、校验签名、备份完整数据、安装并自动重启；页面重新连接并核验实际运行版本后提示成功。
- 支持标准便携包与一次性接入更新器的 Docker Compose 部署；保留登录会话、订阅、端口池、账号密钥、设置和运行数据。
- 安装前允许取消；安装或验证失败时恢复原版本与同一恢复点的数据，提供更新中断后的本机恢复命令。
- 正式发布提供 Ed25519 签名更新清单，绑定安装包校验和、源码提交及应用与核心镜像摘要。

### 修复

- 订阅节点失效后，已配置端口仍显示在列表中并提示全部或部分节点异常；支持异常筛选、编辑时查看和移除失效节点，保留原有端口配置直到用户保存。
- 将间接依赖 `qs` 从 6.15.3 更新到 6.16.0，修复上游报告的查询参数解析与序列化拒绝服务问题。

### 优化

- 推送 `v*` 版本标签后自动完成校验、六平台便携构建、双架构容器测试、GHCR 镜像推送和 Release 发布；保留默认只生成草稿的手动入口，跳过标签删除事件，并继续拒绝覆盖公开版本。
- 浏览器标题随登录状态和当前页面变化；更新 README 封面、下载导航和按操作系统区分的命令示例。
- 源码开发的 Docker 更新命令保留已登记的更新器和数据卷；存在网页更新任务时拒绝重建。
- 新增中英文升级、首次接入和恢复说明。旧版本用户需先手动安装包含更新功能的版本，此后才能从页面更新。

## [1.2.0] - 2026-09-03

### 新增

- 节点实时健康、批量延迟测试与取消，端口验证历史、24 小时成功率和小时失败趋势。
- 默认关闭、具有并发/频率/容量上限的后台检测，检测历史和设置持久化。
- 版本化 `/api/v1` 与包含 18 个操作的 OpenAPI 合同，支持可过期、可撤销的作用域 API 令牌。
- `ppm doctor / backup / restore / ports list / subscriptions refresh` 自动化命令，支持凭据文件、JSON 输出和明确退出码。
- 界面与 API 恢复前的配置差异预检、缺失节点检查、10 分钟签名计划及显式应用；配置变化后拒绝旧计划。

### 修复

- 订阅导入、修改、刷新和删除在 Mihomo 确认重载后才提交；失败时恢复原订阅快照、节点和核心配置，并记录错误，避免无效订阅污染后续端口操作。
- YAML 解析错误仅返回错误代码和位置，补充 UUID、私钥等敏感字段脱敏，并在读取历史审计记录时再次脱敏。
- 诊断正确识别已启用但停止运行的调度器，区分无定时任务和备份恢复期间的主动暂停。

### 优化

- Windows 便携包新增“启动管理器 / 打开管理页面 / 停止管理器”双击入口和中英文入门说明；启动窗口保留首次密码及错误，后台重复启动复用健康实例，失败提示包含日志路径。
- Windows 底层命令行入口移至 `bin/ppm.cmd`，根目录不再附带旧入口或 Unix 脚本；已有自动化脚本需更新路径，数据位置不变。Linux/macOS 继续使用 `./ppm`。
- Release 改为仅手动触发，推送新版本标签不再自动启动草稿构建，避免与正式发布重复运行；保留默认 Draft 和显式正式发布确认。
- 更新中英文 README，补齐部署、可观测性、自动化、发布、贡献、安全、架构、变更记录和第三方声明的双语文档与导航。
- 便携包附带完整双语指南、许可证及 README 图片；增加文档链接、版本一致性和 API 清单回归检查。

## [1.1.0] - 2026-09-02

### 新增

- 增加独立 M2 验收流程，验证六平台签名与 SBOM，并从隔离 GHCR package 按摘要回拉双架构镜像；验收不创建 Release。
- 版本标签推送默认只生成 Draft，正式镜像推送和公开 Release 必须显式手动确认。
- 完善 M2 发布工程：六平台便携包（含 Windows ARM64）、逐包 CycloneDX SBOM、元数据校验和与 SBOM 构建证明。
- 最终便携归档增加首次启动、认证、Mihomo 健康、正常停止和重启测试，测试数据与用户配置隔离。
- 增加 GHCR Linux amd64/arm64 镜像、双架构容器冒烟、BuildKit SBOM/provenance、版本标签防覆盖及摘要固定部署说明。
- 设置页增加源码提交、UTC 构建时间、目标架构和 Node/Mihomo 版本；便携启动支持 `--no-open`。

- 增加口令加密的配置备份、恢复前校验和失败自动回滚，恢复包不包含登录会话与审计日志。
- 增加持久化服务端操作审计、结果筛选、保留策略和敏感信息写入前脱敏。
- 增加数据库、调度器、Mihomo、端口配置和存储空间诊断，以及脱敏诊断文件导出。
- 增加浏览器安全响应头、同源变更请求校验和登录失败记录容量控制。
- 增加发布元数据校验、发行说明提取、制品冒烟测试及历史标签补发能力。

### 优化

- 系统设置页集中提供诊断、备份恢复和应用版本信息，操作记录改为服务端持久化数据。
- 便携包文件名包含应用版本，便于下载、校验和归档。

### 修复

- 修复 Windows Git Bash 环境误选 GNU tar、将盘符当成远程主机导致便携包验收失败的问题；打包、核心解压与验收统一使用系统归档工具。
- 恢复锁覆盖大小写和尾斜杠路由变体，并保持到业务操作结束，避免客户端断连后提前放行配置写入。
- 统一备份导出与导入的字节大小限制，为加密包装预留空间，并在导出时拒绝超限数据。
- 修复 Windows Mihomo ZIP 可能被误当作解包后可执行文件写入便携包的问题。

## [1.0.0] - 2026-09-02

### 新增

- 为订阅库和会话库增加事务化版本迁移，升级已有数据库前自动生成回退备份。
- 为所有 HTTP 请求增加请求 ID，并建立统一、可脱敏的 API 错误响应结构。
- 增加数据库迁移、错误响应和敏感信息脱敏回归测试。
- 增加 Windows、Linux 与 macOS 自包含便携服务包构建，内置 Node.js、Web 管理端和固定版本 Mihomo。
- 增加 `ppm start/stop/status/open` 跨平台入口，支持前台运行与后台服务模式。
- 增加 Mihomo 下载清单和 SHA256 校验，以及多平台 GitHub Actions 构建矩阵。

### 优化

- 将认证、订阅、端口和系统状态路由从服务入口拆分，并提取浏览器端 API 客户端、状态 Hook 与低耦合页面组件。
- 管理服务支持由外部运行时显式启动和优雅停止。
- 便携模式的数据、日志、运行状态和核心目录相互隔离，所有本地端口默认只绑定环回地址。

## [0.1.1] - 2026-09-01

### 新增

- 代理端口列表提供 HTTP 与 SOCKS5 双图标复制按钮，可直接复制完整代理地址。
- 增加 `docker:update` 和 `docker:watch`，支持可靠更新与持续监听本地容器。

### 优化

- 统一界面与双语文档中的订阅、监听和端口配置文案。
- 移除公开源码和测试中的特定订阅品牌及节点名称。

## [0.1.0] - 2026-09-01

首个公开版本，定位为单机、本地代理池。

### 新增

- 原生订阅数据库，支持 URL 与 Mihomo/Clash YAML 导入、刷新和故障回退。
- HTTP、SOCKS5 与 Mixed 本地端口池。
- 手动、主备、延迟优选、稳定哈希和轮询五种节点策略。
- 端口 Listener 检测与多连接出口分布验证。
- 独立 Mihomo 核心、热重载和容器持久化。
- 安全初始化及专用管理员密码重置流程。
- 可选 Clash Verge 远程订阅迁移模式。
- Docker Compose 部署、自动化测试、CI、Dependabot 和社区模板。

### 安全边界

- 默认只绑定 `127.0.0.1`，不支持直接作为公网代理服务使用。
- `.env`、订阅数据库和 Mihomo 生成配置均按敏感数据处理。
- 发布仓库不包含本地订阅、节点凭据、运行数据库、日志或测试截图。
